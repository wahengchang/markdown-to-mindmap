{
  "name": "express-server",
  "version": "1.0.0",
  "description": "Express server with public HTML folder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test e2e/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "playwright": "^1.54.1"
  }
}
